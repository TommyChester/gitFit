<%= form_with(model: blood_sugar_level) do |form| %>
  <% if blood_sugar_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blood_sugar_level.errors.count, "error") %> prohibited this blood_sugar_level from being saved:</h2>

      <ul>
        <% blood_sugar_level.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value %>
  </div>

  <div class="field">
    <%= form.label :occurred_at %>
    <%= form.datetime_select :occurred_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
